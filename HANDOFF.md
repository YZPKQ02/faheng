# AI-Legal-Advisor 项目交接文档

更新时间：2026-07-14（Asia/Shanghai）

## 1. 新会话必须先遵守的工作约定

项目目录：`D:\MyCodex\AI-Legal-Advisor`

用户明确要求：

- 修改 JavaScript/TypeScript 文件后必须运行 `npm test`；Windows 下实际使用 `web` 目录中的 `npm.cmd test`。
- 安装前端依赖优先使用 `pnpm`。
- 添加或调整生产依赖前必须先向用户说明并取得确认。
- 每一轮优化前先给出方案、范围、风险和验收标准，等待用户确认后再修改代码。
- 每一步必须解决真实问题，不要为了增加技术栈而增加技术栈。
- 不读取、打印、提交或覆盖 `.env` 中的密钥；诊断时只能报告“是否配置”。
- 工作区可能包含用户改动，禁止使用 `git reset --hard`、`git checkout --`、`git clean` 等破坏性命令。
- `.git` 元数据曾出现 `not a git repository`，不要依赖 Git 恢复文件。
- Windows 下使用 `npm.cmd`，不要直接运行 npm PowerShell 脚本。
- Vitest 在沙箱内可能因无法写入 `node_modules/.vite/vitest/results.json` 报 `EPERM`。断言通过但进程失败时，应申请在沙箱外重跑，不能删除依赖或关闭缓存规避。

## 2. 我们在做什么

产品名“法衡”，是面向中国大陆劳动争议场景的法律咨询与案件决策辅助 MVP。

目标不是普通聊天机器人，而是：

- 维护案件、事实、证据和对话上下文。
- 区分用户陈述、证据支持、确认事实、争议事实和模型推断。
- 检索并引用法律依据。
- 从劳动者、用人单位和中立裁判视角形成分析。
- 在证据不足、引用缺失或风险较高时进入人工复核。
- 提供可交互的劳动仲裁模拟，让用户练习陈述、答辩、质证和调解。

产品仍是 MVP，不得宣传为律师意见、真实准确率或胜诉概率。

## 3. 当前架构与数据职责

- FastAPI：后端 API。
- Next.js：前端，固定运行在 `http://localhost:3001`。
- SQLite：本地开发和演示。
- PostgreSQL + `psycopg`：生产目标数据库。
- Alembic：生产数据库迁移。
- LangGraph：咨询和案件分析的单轮受控执行，不是长期案件存储。
- PostgreSQL/SQLite：案件、消息、事实、证据、分析、授权、审核和审计的权威存储。
- Redis：尚未引入。只有观测数据证明需要时才可以提方案，并且新增生产依赖仍需用户确认。

后端已实现 OIDC/JWT、租户隔离、案件所有者隔离、人工复核角色门禁。前端仍未接入完整 OIDC/BFF 登录；本地通常使用 `AUTH_ENABLED=false` 的 `local` 身份。

## 4. 本轮会话主要解决了什么

### 4.1 CORS 与前端端口

最初浏览器预检请求：

```text
OPTIONS /cases 400 Bad Request
```

原因是后端默认只允许 `http://localhost:3000`，而前端可能使用 `127.0.0.1` 或其他端口。

已经完成：

- CORS 配置统一从 `settings.cors_origin_list` 获取。
- 自动兼容 `localhost` 和 `127.0.0.1` 的同端口来源。
- 前端 `dev` 和 `start` 固定使用 3001。
- 本地未启用认证时，确保 3001 的两个回环来源可用，即使旧 `.env` 仍保留 3000 配置。
- 增加允许和拒绝来源的预检测试。

关键文件：

- `app/config.py`
- `app/main.py`
- `web/package.json`
- `tests/test_api.py`
- `README.md`

### 4.2 案件模型授权

曾发现 DeepSeek 已配置，但所有协调助手调用都被隐私门禁拒绝：

```text
error_type: consent_missing
```

根因是数据库没有当前案件的模型用途授权。最初只补了 `simulation`，之后发现协调助手还需要 `intake`，案件分析还需要 `analysis`。

已经完成通用授权流程：

- `intake`：案件协调助手。
- `analysis`：多方视角案件分析。
- `simulation`：仲裁模拟。
- 每个入口调用前检查授权。
- 缺少授权时显示通俗的授权弹窗。
- 用户确认后继续执行原操作，不丢失刚才输入的消息。
- 新用途与已有授权合并，不能因为新增 `intake` 而删除 `simulation`。
- 授权说明展示供应商、使用目的、数据范围、隐私处理和撤销权利。
- 不关闭授权门禁，也不替用户自动授权。

关键文件：

- `web/lib/api.ts`
- `web/lib/consent.ts`
- `web/app/page.tsx`
- `web/app/page.test.tsx`
- 后端已有接口：`GET/POST /cases/{case_id}/model-consents`

注意：此前只检查到 `PSEUDONYM_HMAC_SECRET` 未配置，不得读取或输出具体 `.env`。如果登记案件假名，生产环境必须配置独立密钥。

### 4.3 仲裁模拟从单次角色标签升级为受控多角色回合

旧实现并不是真正的多角色交互：每次用户输入只调用一次模型，让模型在仲裁员和单位代理之间选择一个 `speaker`。因此会出现仲裁员说“现在询问单位意见”，但单位代理并没有真正执行。

当前实现已经改为应用层控制的固定协议。

程序性问题，例如“你们是谁”“我没听懂”“流程是什么”：

```text
用户问题 → 仲裁员回答
```

实体争议问题：

```text
用户发言
  → 用人单位代表独立答辩/质证
  → 仲裁员结合双方内容归纳并追问
  → 仲裁助手生成场外建议
```

约束：

- 每轮最多三个模型节点。
- 不允许 Agent 自由循环。
- 每个角色有独立系统提示和结构化输出。
- 任一节点失败时只对该节点规则回退，不让整轮失败。
- 每个节点单独记录 `simulation_agent_call` 审计事件。
- 会话系统元数据保存：`stage`、`round_number`、`last_execution`、`fallback_agents`。
- 当前庭审阶段包括：身份与程序确认、仲裁请求、事实调查、举证质证、辩论、最后陈述/调解。
- 用户本人扮演劳动者；仲裁助手不能替用户发言，也不能创造新事实。
- 模拟内容不会自动写入确认事实。

关键文件：

- `app/services.py`
- `app/agent_contracts.py`
- `tests/test_api.py`

### 4.4 仲裁模拟规则回退

最初模型失败后，无论用户说什么都返回同一段：

```text
你的陈述已经记录……请说明具体请求……
```

现在：

- 角色问题会介绍仲裁员、单位代表、仲裁助手和用户。
- 流程/听不懂问题会用通俗语言解释当前需要回答什么。
- 实体主张会进入单位代表、仲裁员和仲裁助手的受控回合。
- 系统记录 `model`、`hybrid` 或 `rule` 模式及回退角色。
- 前端显示“智能模拟”“部分智能回应”“基础模拟”，不向普通用户展示 fallback、Agent 等术语。

### 4.5 仲裁模拟界面

已经完成：

- 页面固定在 `100dvh`，不会被长内容撑出浏览器底部。
- 顶部固定。
- 庭审对话独立滚动。
- 右侧答题建议独立滚动。
- 输入框和提交按钮始终可见。
- 授权弹窗正文独立滚动，操作按钮固定。
- 显示庭审阶段和回合号。
- 显示本轮真实执行顺序和哪些角色使用了备用回答。
- 隐藏内部系统元数据消息，不把技术状态混入庭审对话。
- 用户要求本轮主要关注桌面端，不需要继续优化移动端。

桌面布局最新调整：

- 右侧答题建议栏宽度为约 292px。
- 会话主区域左右内边距压缩到约 14–30px。
- 每条消息卡片最大宽度为会话区的 74%。
- 短消息按内容自然收缩。
- 仲裁员/单位代表消息在左，用户消息在右。
- 消息卡片不能越过对侧头像应占的空间。
- 角色名显示在头像上方，消息卡片内不再重复显示角色名。

关键文件：

- `web/app/page.tsx`
- `web/app/globals.css`

### 4.6 3D 角色头像

使用 Codex 内置 `imagegen` 生成了四张项目头像：

- `web/public/avatars/arbitrator.png`：仲裁员，深蓝法袍和法槌元素。
- `web/public/avatars/employer.png`：单位代表，酒红/深灰西装和公文包徽章。
- `web/public/avatars/coach.png`：仲裁助手，蓝绿色服装和指南针徽章。
- `web/public/avatars/worker.png`：用户/劳动者，浅蓝服装和文件夹。

生成方式：内置 `imagegen`，提示词统一采用 premium soft-clay 3D app avatar、虚构人物、方形圆角背景、无文字/水印/Logo。

接入方式：

- 使用 Next.js `Image`，显示尺寸约 56×56。
- 角色名由网页清晰渲染在头像上方，不把中文直接生成进图片。
- 图片加载失败时，底层 Emoji 作为备用头像。
- 源 PNG 约 1.6–1.9MB/张，Next.js 会按显示尺寸优化请求。不要直接改回普通 `<img>` 导致加载全部原图。

内置图像生成服务曾出现间歇性网络错误。不要因为一次失败就认为没有生成能力；有限重试即可，不能无限重试，也不能未经用户明确同意切换到需要 `OPENAI_API_KEY` 的 CLI fallback。

### 4.7 仲裁模拟混合式语义路由

- 明确点名和明显实体陈述由应用层直接路由。
- 其他自然表达由结构化语义路由模型判断 `speech_act`、目标角色和响应计划。
- 应用层对白名单、角色边界和置信度进行二次校验；低于 `0.72` 时只由仲裁员澄清。
- 语义路由复用案件授权、假名替换和敏感信息脱敏，只接收限长的近期对话和庭审状态。
- 模型不可用、未授权或失败时退回确定性路由。
- 每轮仍最多三个模型节点；语义路由后的完整回合使用确定性仲裁助手建议控制预算。
- 不引入 AutoGen。`SelectorGroupChat` 更适合开放式协作，会增加逐发言者选择调用；当前受控庭审协议继续使用应用层计划更容易审计。

### 4.8 仲裁模拟退出恢复与明确结束

- 用户可见角色名统一为“仲裁助手”；内部 `worker_coach`、CSS 类名和头像文件名保持不变。
- 顶部“退出”只关闭界面，不结束或删除会话。
- 再次进入同一案件会恢复最近的 `active` 模拟，包括完整 transcript、阶段和回合号。
- 右侧“结束本次模拟”才把会话标记为 `completed`；完成后不能继续写入，下次进入创建新会话。
- `SimulationSession` 新增 `status`、`created_at`、`updated_at`，并提供 Alembic 迁移和 SQLite MVP 兼容升级。
- 不同案件、场景和用户角色之间不会复用模拟会话。

## 5. 之前已经完成且仍需保留的能力

### 5.1 咨询工作流与上下文

- 受控 `plan-execute-react-v1`。
- 当前消息始终最高优先级。
- 规划、事实保存、最多两次法律检索、执行验证和最终回答分阶段执行。
- 模型失败时确定性回退。
- 不保存或展示详细思维链，只保存可审计计划、动作和引用。
- 历史消息最多 12 条、事实最多 30 条、证据最多 20 条，并有字符上限。
- 修复长对话忘记当前问题。
- 修复 `1. 1.`、`2. 2.` 重复追问编号；后端是唯一编号来源，前端只兼容旧数据。

### 5.2 事实与证据安全

- 模型抽取最多写成 `user_stated` 或 `inferred`，不能直接写 `confirmed`。
- 文本匹配只能产生候选关联，不能把事实自动升级为 `evidence_supported`。
- 删除案件会删除关联反馈。
- 删除审计不保存原始案件标题，只保存不可逆指纹。

### 5.3 法律检索

- 区分相关性与法律层级/地域权重。
- 无关查询可以返回空结果。
- 不会仅因层级加分返回无关法条。
- 当前仍是演示级中文 n-gram/关键词检索，不是完整生产 RAG。

### 5.4 数据库

- Alembic 迁移链已存在。
- PostgreSQL 使用同步 SQLAlchemy `Session` + `psycopg`。
- PostgreSQL 不能在应用启动时 `create_all`，必须先执行 Alembic。
- 本地 SQLite 继续使用兼容逻辑。
- 不要对已有数据库盲目 `stamp head`。

### 5.5 隐私与可观测性

- 模型出站前遮蔽身份证、手机号、固定电话、邮箱和银行卡号。
- 案件假名使用作用域 HMAC 指纹，不重复保存原文。
- 模型调用顺序：授权检查 → 案件假名 → 规则脱敏 → 外部模型。
- 指标不保存 prompt、查询原文、模型回答、案件事实或敏感实体。
- `/internal/metrics` 只允许审核角色访问。

## 6. 当前验证状态

最近一次完整验证：

- `ruff check app tests`：通过。
- `pytest`：54 项通过。
- `npm.cmd test`：9/9 通过。
- `npm.cmd run build`：通过。
- 已知警告：Starlette TestClient 与当前 httpx 的弃用警告；不要为了清理警告直接升级生产依赖。
- Vitest 仍会输出 Vite CJS API 弃用提示；不要仅为清理提示盲目升级依赖。

## 7. 当前卡在哪里

没有代码级 blocker，但仍有以下缺口：

1. 最新 3D 头像和消息宽度已经构建通过，但还需要用户在实际浏览器中进行视觉验收，尤其是 1366px 和 1920px 桌面宽度。
2. 仲裁模拟当前是一次 HTTP 请求内顺序执行最多三个模型调用，前端只能显示“正在准备回应”，不是真正的逐节点 SSE 流式事件。
3. 多角色模型调用增加成本和延迟，需要用现有观测数据评估，不能直接加 Redis/Celery。
4. 前端仍没有完整 OIDC/BFF 登录、token 刷新和安全会话。
5. 前端虽然有按用途授权弹窗，但没有完整的授权历史、撤销和假名管理页面。
6. 真实律师复核数据不足，不能报告准确率或胜诉概率。
7. 法律知识库仍是 MVP 演示数据，缺少完整版本、地方规则、失效追踪和专业复核。
8. 尚未在真实 PostgreSQL 环境完成迁移、并发、连接池压力、备份与恢复验证。
9. 当前 SSE 咨询流仍是现有实现；仲裁模拟还不是可断线重连、可事件重放的后台任务流。
10. 用户本轮明确表示暂时不需要继续考虑移动端；不要自行扩大范围去重做移动端。

## 8. 下一步计划

新会话必须先给方案并等待确认，不能直接修改。

建议优先顺序：

### P0：桌面端视觉验收与小范围修正

- 请用户重启前端并截图 1366px/1920px 下的新仲裁模拟。
- 检查角色名是否在头像上方清晰显示。
- 检查短消息是否自然收缩、长消息是否不超过 74%。
- 检查 3D 头像在 56px 下的裁切和辨识度。
- 只根据实际截图做一次小范围 CSS 修正，不继续大改版。

### P0：仲裁模拟真实模型回合验证

- 新建一个已授权 `simulation` 的案件。
- 发送实体主张，确认执行顺序为：单位代表 → 仲裁员 → 仲裁助手。
- 发送“你们是谁”，确认只执行仲裁员。
- 检查 `simulation_agent_call` 审计事件不超过 3 条/回合。
- 检查模型失败时只标记对应角色备用回答。

### P1：授权管理 UI

- 展示当前案件已授权用途和数据范围。
- 支持撤销授权。
- 支持案件假名登记与查看。
- 不自动扩大授权范围。

### P1：前端 OIDC/BFF 登录

- Next.js BFF + OIDC PKCE。
- 浏览器使用 HttpOnly、Secure、SameSite Cookie。
- 不把长期 JWT 放入 localStorage。
- 可能涉及新生产依赖，必须先确认。

### P1：真实 PostgreSQL 与评测

- 受控 PostgreSQL 执行 `alembic upgrade head`。
- 验证索引、外键级联、租户过滤、并发写入、连接池、备份恢复。
- 建立律师双人复核、第三人仲裁的脱敏案例集。

## 9. 绝对不要再踩的坑

1. 不要让模型和渲染层同时编号；后端是新追问唯一编号来源。
2. 不要把最近对话的 Python repr 整块塞给模型；必须分层传递当前问题、历史、事实和证据。
3. 不要展示或保存详细思维链；只展示可验证计划、动作、引用和结果。
4. 不要把模型抽取直接写成 `confirmed`。
5. 不要因为文本命中就把事实升级为 `evidence_supported`。
6. 不要把置信度写成胜诉概率。
7. 不要把模型模拟标注宣传成律师标注。
8. 不要让 Agent 无限循环；必须限制节点、工具次数、超时、成本和失败回退。
9. 不要把 Redis 当主数据库，也不要在没有指标证据时引入 Redis。
10. 不要把 JWT 长期放入浏览器 localStorage。
11. 不要把敏感原文写入指标、审计、日志或 Redis key。
12. 不要在生产 PostgreSQL 启动时 `create_all`。
13. 不要对存量数据库盲目 `stamp head`。
14. 不要使用 `asyncpg` 搭配当前同步 SQLAlchemy Session；当前生产驱动是 `psycopg`。
15. 不要为了清理弃用警告直接升级生产依赖。
16. 不要读取或输出 `.env`、模型密钥、JWT 密钥或 HMAC 密钥。
17. 不要直接从 Next.js 页面模块导出测试工具函数；曾导致 `next build` 因页面模块额外导出失败。工具函数放 `web/lib/`。
18. 不要把“仲裁员说接下来询问单位意见”当作单位代表已经执行；必须检查 `last_execution` 和真实单位代表消息。
19. 不要用一个模型调用加不同角色标签冒充多角色交互。
20. 不要让仲裁助手在庭上替劳动者发言；它只能更新右侧建议。
21. 不要在缺少 `intake`/`analysis`/`simulation` 授权时调用外部模型，也不要静默退回而不告知用户。
22. 新授权必须与已有用途合并，不能授权一个用途时撤销另一个用途。
23. 不要把角色中文直接生成到头像图片里；小尺寸下中文容易失真。角色名由网页渲染。
24. 不要把生成头像改成普通 `<img>` 全量加载大 PNG；当前使用 Next.js `Image` 做尺寸优化。
25. 内置 `imagegen` 失败时不要未经用户同意切换到需要 `OPENAI_API_KEY` 的 CLI fallback。
26. 不要未经确认连续推进下一轮优化；本轮交付后，下轮先给方案。

## 10. 关键文件索引

- `app/workflow.py`：咨询主工作流。
- `app/conversation_memory.py`：上下文限长与分层装配。
- `app/strategy_workflow.py`：劳动者代理、单位代理、中立裁判、安全审核分析图。
- `app/services.py`：案件分析服务、仲裁模拟受控多角色回合、文书生成。
- `app/agent_contracts.py`：结构化模型输出协议。
- `app/model_gateway.py`：DeepSeek 调用、重试、授权/隐私门禁和遥测。
- `app/privacy.py`：规则脱敏、HMAC 和案件假名替换。
- `app/privacy_governance.py`：案件模型授权上下文。
- `app/auth.py`：OIDC/JWT 和 Principal。
- `app/observability.py`：模型/检索指标。
- `app/authorities.py`：法律依据检索。
- `app/main.py`：FastAPI 接口和权限过滤。
- `app/models.py`：数据模型。
- `web/app/page.tsx`：主界面、授权门禁、仲裁模拟 UI 和头像接入。
- `web/app/globals.css`：全部前端样式，重点关注 simulation/hearing/avatar/message-card。
- `web/lib/api.ts`：前端 API 客户端。
- `web/lib/consent.ts`：授权用途与数据范围合并。
- `web/public/avatars/`：四张 3D 角色头像。
- `tests/test_api.py`：CORS、模拟顺序、最大调用数和角色分流测试。
- `web/app/page.test.tsx`：前端 API 与授权合并测试。
- `README.md`：运行、端口、认证、迁移、隐私和指标说明。

## 11. 常用命令

后端：

```powershell
.\.venv\Scripts\python.exe -m ruff check app tests
.\.venv\Scripts\python.exe -m pytest
```

前端：

```powershell
cd web
npm.cmd test
npm.cmd run build
pnpm dev
```

前端固定访问：

```text
http://localhost:3001
```

数据库迁移：

```powershell
.\.venv\Scripts\python.exe -m alembic upgrade head
.\.venv\Scripts\python.exe -m alembic check
```

## 12. 新会话建议开场

先告诉用户：已经阅读交接文档，将继续遵守“先方案、确认后执行”，不会为了增加技术栈而改造，也不会读取或输出 `.env` 密钥。

如果用户没有给出新方向，优先建议先重启前端，对最新桌面仲裁模拟界面做一次实际截图验收；不要自动开始下一轮代码修改。

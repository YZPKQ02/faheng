# HANDOFF：法衡 AI 劳动争议法律助手

更新时间：2026-07-21
仓库：`YZPKQ02/faheng`
工作分支：`agent/hybrid-plan-execute-workflow`

## 1. 我们在做什么

这是一个面向中国大陆劳动争议的法律信息、案件准备和仲裁模拟系统。当前主线是：

1. 用可审计的多 Agent 工作流完成劳动者立场、单位抗辩、中立评估和安全审查。
2. 让诉求收集阶段的劳动者代理延续到仲裁模拟，并继承案件记忆。
3. 从官方来源构建版本化法律语料，通过关键词与 Qwen3 向量执行混合检索。
4. 保持事实边界、法律版本、引用白名单、人工复核和模型授权可追溯。

系统仍是本地试点，不是可直接承担法律责任的生产服务。

## 2. 已经完成

### 2.1 LangGraph 策略分析

- 诉求收集工作流采用 Plan-and-Execute：观察、规划、事实持久化、有界法律检索、执行校验和最终答复。
- 策略分析已经从线性 `劳动者 → 单位 → 裁判 → 安全` 升级为 `legal-debate-v2`：

```text
              ┌→ 劳动者代理 ─┐
START ────────┤              ├→ 串行审计 → 中立裁判者 → 安全审查
              └→ 单位代理 ──┘
```

- 劳动者代理和单位代理读取同一份事实、证据和候选法条，独立并行生成观点；单位代理不再读取劳动者观点。
- 每个模型节点使用独立 `ModelGateway`，避免 `last_telemetry` 串线。
- 并行分支不写数据库；双方汇合后由 `record_party_tasks` 串行记录审计，避免共享 SQLAlchemy Session。
- 状态新增 `worker_execution` 和 `employer_execution`，记录完成/回退、耗时和错误类型。
- 任一方或双方模型失败时，均使用各自确定性回退并继续进入裁判与安全审查。
- 新增 `tests/test_strategy_workflow.py`，用线程屏障验证双方确实并行、裁判等待双方、独立 Gateway、单方/双方失败以及审计协议。

### 2.2 劳动者代理与仲裁模拟

- `worker_counsel` 是从诉求收集延续到仲裁模拟的同一角色，长期记忆持久化在 PostgreSQL；模拟会话固定一份记忆快照和版本。
- 仲裁阶段由劳动者本人发言，劳动者代理只提供场外建议，不替用户确认或创造事实。
- 每轮最多显示 4 条建议和 4 条可直接填入回答框的答案；填入不会自动发送。
- 所有可见仲裁角色输出通过 SSE 流式展示。
- 多轮短回答会稳定进入单位代理和仲裁员流程，不再因语义路由误判而让单位代理沉默。
- 仲裁员重复同一问题时跳过劳动者代理模型调用并沿用已有建议；新建议会参考并过滤上一轮输出。
- 模型的内联编号、紧贴编号和加粗编号会被前端拆成独立行显示。

### 2.3 法律语料与检索

- 修复法规附件/附录被错误并入最后一条的问题，生成 `data/legal/pilot_v2/`。
- 建立法律文件、版本、自然法条块、向量和显式版本替换/失效关系。
- 全国人大法律语料位于 `data/legal/npc_laws_v1/`：8 部现行全国性法律，658 个自然法条块。
- 本地试点库上次确认共有 1772 个 Qwen 1536 维向量；导入和索引具有幂等性。
- 检索会在查询前过滤生效日期、失效日期、版本状态和地域；模型只能引用本轮候选 `authority_id`。
- Qwen3 Embedding 通过本机 llama.cpp 运行，原始 4096 维输出经 MRL 截断并重新归一化为 1536 维后入库。

### 2.4 数据库与验证

- 本地 Docker 只运行 PostgreSQL 16 + pgvector，监听 `127.0.0.1:5433`；FastAPI、Next.js 和 Qwen 服务运行在 Windows 本机。
- 当前迁移链最后一个版本为 `d2f6a9b3c841`，包含劳动者代理记忆、冗余唯一约束修正和模拟建议答案字段。
- 本次 LangGraph 改造不需要新增数据库迁移，也没有改变现有 API 响应契约。
- 最近一次完整验证：后端 81 项通过，前端 12 项通过，Ruff 和 `git diff --check` 通过。

## 3. 当前卡在哪里

没有已知代码阻塞。

本次并行策略图已经完成并通过确定性测试，但尚未用真实 DeepSeek 请求验证外网延迟、供应商限流和真实并行耗时。真实收费模型调用必须在获得明确授权并确认案件模型数据同意后执行。

如果当前分支尚未合并，先检查 GitHub 上该分支相对 `main` 的状态和最新草稿 PR，不要重复创建 PR。

## 4. 下一步计划

按顺序执行：

1. 合并并行策略图改动；在 PostgreSQL 试点环境运行一次 API 冒烟测试，确认四个 AgentTask 和 `legal-debate-v2` 审计均写入。
2. 获得明确授权后，对一个不含真实敏感信息的测试案件执行一次 DeepSeek 端到端测试，记录双方分支耗时、总耗时、回退和限流情况。
3. 根据真实指标决定是否需要并发上限或供应商级限流；不要仅凭理论引入 Redis。
4. 若需要更强对抗，再单独设计可选的第二轮交叉反驳；默认流程继续保持一次双方独立观点，以控制成本。
5. 后续语料扩展顺序：最高法劳动争议司法解释 → 人社部规章/规范性文件 → 明确选定地区的地方规定。每批都必须版本校验、导入、索引和开发集评测。

## 5. 绝对不要再踩的坑

1. **不要让并行 LangGraph 节点共享 SQLAlchemy Session。** 并行节点只返回状态，数据库写入放到汇合后的串行节点，或使用严格隔离的独立事务。
2. **不要让并行模型节点共享 `ModelGateway`。** `last_telemetry` 是可变状态，共享会造成指标和错误归属串线。
3. **汇合必须使用 `add_edge([worker, employer], join)`。** 不要用两个普通入边替代明确汇合，否则可能产生错误触发语义。
4. **单位首轮观点不能读取 `worker_argument`。** 双方首轮必须基于同一份不可变案件材料独立生成；需要交叉反驳时另建可选第二轮。
5. **不要并行运行会重置同一个 SQLite 测试库的多个 pytest 进程。** 本次曾因此发生 `drop_all` 删表竞争；数据库测试必须在同一 pytest 进程串行运行。
6. **不要读取、输出、覆盖或提交 `.env` / `.env.local`。** 真实密钥不得进入仓库、日志、测试报告或交接文档。
7. **PostgreSQL 不得使用 `create_all`、盲目 `stamp head` 或跳过迁移。** 必须通过 Alembic，并先检查 `current` 与 `check`。
8. **不要给 pgvector 建 4096 维 `vector` HNSW 索引。** 当前固定使用 Qwen MRL 1536 维；pgvector `vector` HNSW 上限为 2000 维。
9. **不要把确定性哈希向量称为语义检索。** 它只用于测试和无外部服务回退。
10. **不要根据冻结测试集继续调参或直接修改冻结题目。** 只能使用开发集选参；标签修正必须升数据集版本并留审计记录。
11. **不要把同名官方 LawID 全部当成不同的现行法律。** 必须按规范标题、版本日期和版本链去重并复核。
12. **不要在仲裁模拟恢复“所有 Agent 都回答”的广播逻辑。** 由发言权路由器、当前阶段和待回答对象决定角色；仲裁员负责控场。
13. **退出仲裁模拟不能删除或完成会话。** 退出只关闭界面，active 会话必须能够恢复。
14. **模型输出不能绕过事实和引用边界。** 不得把推断当事实；引用必须属于本轮有效候选 ID。
15. **不要声称 AI 审核等于执业律师审核，也不要输出伪精确胜诉率。** 高风险结论必须保留人工接管路径。
16. **`compose.trial.yml` 的本地 trust 配置不能用于生产。** 它仅绑定回环地址并服务本地试点。

## 6. 新会话开始时

1. 先读本文件，再运行 `git status -sb` 和 `git log -5 --oneline`。
2. 不读取任何 `.env` 文件；需要配置时只参考 `.env.example`。
3. 修改 JavaScript 后运行 `npm test`；安装依赖优先使用 pnpm；新增生产依赖前先征得确认。
4. 完成代码修改后至少运行：

```powershell
.\.venv\Scripts\ruff.exe check app tests
.\.venv\Scripts\python.exe -m pytest -q
cd web
npm.cmd test
```

5. 数据库相关改动额外运行 Alembic `current`、`upgrade head` 和 `check`；不得触碰冻结评测集。

# HANDOFF：法衡 AI 劳动争议法律助手

更新时间：2026-07-16
仓库：`YZPKQ02/faheng`
当前分支：`agent/hybrid-plan-execute-workflow`

## 1. 我们在做什么

这是一个中国大陆劳动争议法律咨询与仲裁模拟系统。当前主线任务有两部分：

1. 让多智能体对话从固定流程升级为有状态的智能选角：只有当前最适合的角色回答，必要时由仲裁员控场，避免用户随口提问时所有 Agent 抢答。
2. 建设可追溯的法律 RAG：从官方来源导入版本化法条，使用关键词与 Qwen3 语义向量混合检索，回答只能引用本轮有效候选法条。

系统仍是试点版本，不是可以直接上线承担法律责任的生产系统。

## 2. 已经完成

### 2.1 多智能体与工作流

- 已实现 Plan-and-Execute：先生成结构化执行计划，再按依赖执行事实澄清、争点分析、证据分析、法律检索和结论生成。
- 节点内部保留 ReAct 式工具调用与观察结果回填。
- 仲裁模拟已改为智能选角/控场机制，解决多个 Agent 不知道是否应该回答的问题。
- 仲裁模拟退出后不会删除历史；再次进入会恢复 active 会话。
- 通用“答题助手”已改成与业务一致的“诉讼助手”。
- 高风险结论保留人工复核、授权、审计和失败回退机制。

### 2.2 版本化法律知识与 RAG

- 已新增 `LegalDocument`、`LegalDocumentVersion`、`LegalChunk`、`LegalChunkEmbedding`。
- Alembic 迁移：
  - `c0a7d9e6b421`：版本化法律知识结构。
  - `d4b8f2a1c935`：pgvector 向量表。
  - `e8f1a4c7d260`：固定 1536 维并创建余弦 HNSW 索引。
- 已实现官方来源白名单、JSONL 导入、内容哈希幂等、版本区间检查和审计事件。
- 已实现关键词检索、Qwen3 语义检索和 RRF 混合检索。
- 检索前硬过滤生效日期、失效日期、版本状态和地域。
- LLM 只能返回本轮候选 `authority_id`；应用层会再次校验，拒绝越界或伪造引用。
- deterministic 哈希向量只用于测试/离线回退，不再被当作真实语义检索器。

### 2.3 本地 Qwen3 Embedding

- 模型：`D:\AIModels\Qwen3-Embedding-8B\Qwen3-Embedding-8B-Q4_K_M.gguf`。
- llama.cpp：`D:\AIModels\llama.cpp`，CUDA 12，版本 b10037。
- 启动脚本：`scripts/start_local_embedding.ps1`。
- 服务：`127.0.0.1:8080`，Embedding-only、last pooling、4096 上下文、单并发。
- 模型原始输出 4096 维；应用按 MRL 截断并重新归一化为 1536 维后入库。
- 非敏感本机连接参数放在被 Git 忽略的 `.env.local`，不要读取、覆盖或提交现有 `.env`。

### 2.4 1000 条级官方法规试点库

- Docker 试点库：`compose.trial.yml`，PostgreSQL 16 + pgvector，只监听 `127.0.0.1:5433`。
- 来源：司法部国家行政法规库。
- 规范化语料：25 部现行行政法规、1092 个自然法条块。
- 数据文件：
  - `data/legal/pilot/corpus.jsonl`
  - `data/legal/pilot/source_manifest.json`
- 数据库当前共有 1097 个法条块和 1097 个 1536 维向量，包含原有 5 个演示法条。
- 导入和索引均幂等：重复导入新增 0，重复索引 `indexed=0`。
- 原始网页缓存在 Git 忽略的 `data/raw/xzfg/`。

### 2.5 检索金标准与 AI 法律审核

- 初始生成 120 道候选题，覆盖 8 个主题；包含 96 道单法条题和 24 道跨法规题。
- 所有题带官方 URL、证据摘录、适用日期和标准引用。
- AI 法律证据审核后的 v2 位于 `data/evaluation/pilot_gold/reviewed_v2/`：
  - 审核 120 题。
  - 直接通过 88 题。
  - 修改后通过 28 题。
  - 剔除 4 题。
  - 最终 approved 116 题。
  - 开发集 76 题，重新冻结测试集 40 题。
- 明确关联的《劳动合同法》《劳动争议调解仲裁法》《船员条例》《对外承包工程管理条例》已在全国人大或司法部官方库核对。
- reviewed v2 冻结集结果：

| 检索方式 | Recall@10 | nDCG@10 |
|---|---:|---:|
| 关键词 | 0.950 | 0.9372 |
| 纯语义 | 0.9875 | 0.9590 |
| 混合 RRF | 0.975 | 0.9532 |

- 混合相对关键词：39 题持平、1 题更好、0 题更差。
- 开发集仍选择默认参数：关键词权重 1.0、语义权重 1.0、`rrf_k=60`。
- `test.json` 的 SHA-256 同时写入审核报告和调优报告，防止静默修改冻结集。

## 3. 当前卡在哪里

没有阻止本地运行的代码故障。当前真正未完成的是以下事项：

1. **附录解析缺陷**：采集器会把法规附录续接到最后一条。已发现《女职工劳动保护特别规定》的附录被并入第十六条，对应金标准题已剔除，但解析器本身尚未修复。
2. **语料覆盖不足**：当前主要是 25 部行政法规，尚未系统覆盖全国性法律、最高法劳动争议司法解释、部门规章及地方规定。
3. **生产安全未完成**：`compose.trial.yml` 使用仅限本机的免密数据库配置；默认认证关闭。它只能用于本地试点。
4. **真实生成模型未做最终冒烟测试**：后端显示 DeepSeek 已配置，但本轮没有实际发送收费请求，因此未验证密钥、额度和外网稳定性。
5. **AI 审核不是律师签署**：116 题只能称为 AI 法律证据与一致性审核。标准引用是最低预期依据，不表示已经穷尽所有适用规范。

## 4. 下一步计划

按顺序执行，不要跳步：

1. 修复法规解析器：把“附录”“附件”建成独立 locator，并增加回归测试。
2. 以新数据版本重新构建语料；不要静默覆盖 reviewed v2，也不要直接修改冻结测试集。
3. 设计版本替换/失效流程，避免修正后的同名法规与旧版本同时处于 active。
4. 扩展官方语料：全国人大法律 → 最高法司法解释 → 人社部规章/规范性文件 → 明确选定地区的地方规定。
5. 每批扩容后重新建索引，生成新版本金标准，并只用开发集调参；测试集只在参数冻结后评测一次。
6. 做生产化：正式 PostgreSQL 凭据、OIDC/RBAC、备份恢复、迁移发布流程、模型调用配额与监控。
7. 在获得明确授权后做一次真实 DeepSeek 端到端冒烟测试。

## 5. 绝对不要再踩的坑

1. **不要给 pgvector HNSW 建 4096 维 `vector` 索引。** pgvector 的 HNSW `vector` 上限是 2000 维。本项目固定使用 Qwen3 MRL 截断、重新归一化后的 1536 维。
2. **不要把官方检索返回的同名 LawID 全部当成现行不同法规。** 搜索结果会包含历史沿革版本；必须按规范标题和版本日期去重，并人工复核版本链。
3. **不要为了凑数量切碎、复制或拼接无关法条。** 法条块保持自然“第×条”边界；跨法规题必须明确是两个独立法律问题，不能虚构属于同一争议。
4. **不要根据冻结测试集继续调参。** 只能用开发集选权重；测试集结果不好也不能回头修改参数或题目。发现标签事实错误时必须升数据集版本并留下记录。
5. **不要把 deterministic 哈希向量称为语义检索。** 它在低维下会产生无关碰撞，只能用于测试和无外部调用回退。
6. **不要声称 AI 审核等于执业律师审核。** `ai_reviewed` 与 `expert_approved` 必须区分；最低预期引用也不等于完整法律意见。
7. **不要在仲裁模拟中恢复“所有 Agent 都可回答”的广播式逻辑。** 由 Selector/路由器根据阶段、角色权限和上一轮发言决定下一位发言者；仲裁员负责歧义控场。
8. **不要在退出仲裁模拟时删除或完成会话。** 退出只是关闭界面，active 会话应保留并可恢复。
9. **不要读取、输出、覆盖或提交 `.env`。** 本机非敏感配置用 `.env.local`；任何真实密钥都不得进入仓库、日志或测试报告。
10. **不要把 `compose.trial.yml` 的 trust 认证带到生产。** 它只绑定本机回环地址，仍然只能作为试点环境。
11. **不要对已有 PostgreSQL 盲目 `create_all`、`stamp head` 或跳过迁移。** PostgreSQL 必须执行 Alembic；先检查 `alembic current` 和 `alembic check`。
12. **本地 llama.cpp 单槽服务批量过大时可能 503。** 当前稳定批大小是 4；索引按批提交且可断点续建，不要改回一次性大事务。
13. **Windows PowerShell 可能阻止 `npm.ps1`。** 使用 `npm.cmd` 或 `pnpm`。沙箱内 Vitest 缓存可能报 EPERM，但在正常 Windows 权限下测试和构建均可通过。

## 6. 当前验证状态与运行方式

最后一次完整验收：

- Ruff 通过。
- 后端测试 66/66 通过。
- 前端测试 9/9 通过。
- Next.js 生产构建通过。
- 后端真实启动后 `/health` 返回 `ok`。
- 前端生产服务首页返回 HTTP 200。
- Docker PostgreSQL 状态 healthy。
- Alembic 位于 `e8f1a4c7d260 (head)`，`alembic check` 无差异。
- Qwen Embedding `/health` 返回 `ok`。
- Python `pip check` 和前端依赖树检查通过。

本机启动：

```powershell
docker compose -f compose.trial.yml up -d
powershell -ExecutionPolicy Bypass -File scripts\start_local_embedding.ps1

$env:DATABASE_URL='postgresql+psycopg://legal_trial@127.0.0.1:5433/legal_trial'
.\.venv\Scripts\uvicorn.exe app.main:app --host 127.0.0.1 --port 8000
```

另开终端：

```powershell
cd web
npm.cmd run dev
```

访问 `http://localhost:3001`，API 文档为 `http://localhost:8000/docs`。
